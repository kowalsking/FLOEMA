extends ../base.pug

block variables
  - var template = 'detail'

block content
  .detail
    .detail__wrapper
      figure.detail__media
        img.detail__media__image(alt=product.data.model.alt src=product.data.model.url)

      .detail__information
        p.detail__information__collection=product.data.title
